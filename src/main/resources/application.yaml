spring:
  application:
    name: DOSW-Library
  data: 
    mongodb:
      uri: mongodb+srv://andress_db_user:Hola123@cluster0.salamb7.mongodb.net/?appName=Cluster0
      database: dosw_library
      
  datasource:
    url: jdbc:postgresql://localhost:5432/dosw_library
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  security:
    user:
      name: admin
      password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  jwt:
    secret: MySuperSecretKeyForJWT1234567890
    expiration: 3600000
server:
  port: 8443
  ssl:
    enabled: true
    key-store: classpath:keystore.p12
    key-store-password: library
    key-store-type: PKCS12
    key-alias: dosw-library
